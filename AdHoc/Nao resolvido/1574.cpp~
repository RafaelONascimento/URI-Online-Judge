#include <cstdio>
#include <cstdlib>

#define MAX 200

int main(){
  int n, i,oper[MAX], pos = 1;;
  char inst[MAX];

  char teste[MAX] = 'SAME AS 1';
  
  /*scanf("%d",&n);

  memset(inst,0,sizeof(inst));
  
  for(i = 0; i < n; i++){
    fgets(inst,MAX,stdin);
    getchar();

    if(inst[0] == 'L'){
      pos -= 1;
      oper[i] = -1;
    }
    else if(inst[0] == 'R'){
      pos += 1;
      oper[i] = 1;
    }
    else if(inst[0] == 'S'){
      if(strlen(inst) == );
    }
  }

  */
  
}
